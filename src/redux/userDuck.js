//constant 
let initialData = {
    loggedIn: false
}
let LOGIN = "LOGIN"

//REDUCER
export default function reducer(state = initialData, action) {
    switch (action.type){
        case LOGIN: 
            default:
                return state
    }
}

//action